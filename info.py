import traceback, pandas as pd; CSV_COLS = ['TransactionID', 'TransactionAmt', 'TransactionDT', 'ProductCD', 'card1', 'card4', 'addr1', 'addr2', 'P_emaildomain', 'isFraud']; TRANSACTION_FILE='data/train_transaction.csv'; LIVE_CSV_PATH='data/live_transactions.csv'; \ntry:\n    df_transactions = pd.read_csv(TRANSACTION_FILE, usecols=CSV_COLS, nrows=50_000)\n    df_transactions.fillna(0, inplace=True)\n    total = len(df_transactions)\n    fraud_count = int(df_transactions['isFraud'].sum())\nexcept Exception as e:\n    traceback.print_exc()
